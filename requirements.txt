openai>=1.0.0
pillow>=10.0.0
python-dotenv>=1.0.0
numpy<2

# Phase 2 — diffusion inpainting attacks
torch==2.2.2
diffusers==0.30.3
transformers==4.44.0
accelerate

# Phase 3 — surrogate-model PGD transfer (SigLIP + CLIP)
protobuf
sentencepiece
